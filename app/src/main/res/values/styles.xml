<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TextView">
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textColorHint">@color/colorWhiteTransparent</item>
        <item name="android:textSize">20sp</item>
        <item name="android:gravity">center_vertical</item>
    </style>

    <style name="SelectionButton" parent="TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_marginTop">@dimen/default_margin_value</item>
        <item name="android:layout_marginStart">@dimen/default_margin_value</item>
        <item name="android:layout_marginEnd">@dimen/default_margin_value</item>
        <item name="android:background">@drawable/round_corner_background_primary</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="CardViewText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/x_small_margin</item>
        <item name="android:layout_marginStart">@dimen/default_margin_value</item>
        <item name="android:layout_marginEnd">@dimen/default_margin_value</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:textColor">@color/colorGrayDark</item>
    </style>

</resources>
